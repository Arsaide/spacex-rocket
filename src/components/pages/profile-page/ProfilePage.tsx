import ProfileSection from './sections/profile-section/ProfileSection.tsx';

const ProfilePage = () => {
    return (
        <>
            <ProfileSection />
        </>
    );
};

export default ProfilePage;
